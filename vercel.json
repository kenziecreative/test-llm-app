{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../packages/engine && npm install && npm run build && cd ../../web && npm install && npm run build",
  "outputDirectory": ".next",
  "installCommand": "echo 'Using custom buildCommand'",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}
